# `app.rs` — 测试应用模块（占位）

## 文件位置
`platform/script/test/src/app.rs`

---

## 说明
该文件当前完全为空（0 行）。在 `makepad_script_test` crate 中，原本可能计划放置一个可运行的 Splash 应用示例（类似 Makepad 的 `app_main!` 模式），但目前未实现。

该文件的存在可能仅作为未来扩展的预留占位，或者作为 Rust 模块树的声明占位。实际测试逻辑全部位于 `main.rs` 中。
