# fonts.rs — Chinese Bold 2 font script registration (placeholder)

**File path**: `widgets/fonts/chinese_bold_2/src/fonts.rs` (4 lines)
**Core purpose**: Empty script module for Chinese Bold 2 font registration.

## Content
- Declares an empty `script_mod!{}` block — the font data and script resources are loaded at runtime through the parent crate's `script_mod` path rather than being baked into this module at compile time
